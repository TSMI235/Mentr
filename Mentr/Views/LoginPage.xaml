<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Mentr.Views.LoginPage">
    <ContentPage.Content>
        
        <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    
                </Grid.RowDefinitions>
                <BoxView BackgroundColor="Transparent" Grid.Row="0"/>
            </Grid>
            <Label Text="Mentr" TextColor="DarkOrange" FontSize="Title"
                   HorizontalOptions="Center" VerticalOptions="Start"
                   FontFamily="Callibri"/>
            <Entry  TextColor="Black" FontFamily="Callibri" Placeholder="Username" Keyboard="Text" x:Name="Entry_Username"/>
            <Entry  Keyboard="Text" IsPassword="True" FontFamily="Callibri"  Placeholder="Password" x:Name="Entry_Password"/>
            <Button Text="Login" Clicked="Button_Clicked" FontFamily="Callibri"  HorizontalOptions="Center" VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>